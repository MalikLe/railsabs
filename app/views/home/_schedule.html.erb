<table class='table table-striped table-borderless table-responsive'>
  <thead class='table-dark'>
    <tr>
      <th>Nom</th>
      <th>Prénom</th>
      <th>Classe</th>
      <% days = ['Lu', 'Ma', 'Me', 'Je', 'Ve'] %>
      <% for i in 0..4 %>
	  	<th colspan="8">
	  		<%= days[i] + " " + (@requested_week.start_date + i).strftime("%d.%m") %>
	    </th>
      <% end %>
    </tr>
    <tr>
      <th></th>
      <th></th>
	  <th></th>
      <% days = ['Lu', 'Ma', 'Me', 'Je', 'Ve'] %>
      <% for i in 0..4 %>
	  	
	  		<% for j in 1..8 %>
  				<th class='text-center'>
            		<%= j %>
          		</th>
      		<% end %>
	    
      <% end %>
    </tr>
  </thead>

  <tbody>

    <% @students.each do |student| %>
      <tr>
        <td>
        	<%= link_to edit_student_path(student) do%>
        		<%= student.last_name  %>
        	<%end%>
        </td>
        <td>
        	<%= link_to edit_student_path(student) do%>
        		<%= student.first_name %>
        	<%end%>
        </td>
        <td><%= student.class_name %></td>

        <% for j in 1..days.length() %>
        
            
            	<% for j in 1..8 %>
      				<td class='text-center'>
                		<button type="button" class="btn btn-outline-warning btn-sm">A</button>
              		</td>
      			<% end %>
           
        
        <% end %>
        


        
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Enregistrer', new_student_path, class:'btn btn-primary' %>
<br/>
<br/>